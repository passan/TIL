🧠 과거 TIL 기록([26-03-14_02.md](file:///D:/Opsidian/Development_Docs/02_TIL_Raw/Python/%EC%97%85%EB%8D%B0%EC%9D%B4%ED%8A%B8/26-03-14_02.md))을 검토하여 **[데이터 검증 방식의 오해 소지]**를 확인했습니다. 이전의 보고는 인프라 작동 여부를 위한 '기능 검증'이었으며, 본 보고서에서는 실제 운영 코드에 매핑된 실시간 변수 데이터를 공개하여 '로직 정합성'을 증명합니다.

## 1. 실제 운영 코드 내 실시간 데이터 매핑 현황 (No Mock)

### 1) RuleBasedEngine (룰 기반 엔진)
- **파일**: [rule_based.py](file:///d:/Antigravity/Stock_Game/app/engines/rule_based.py)
- **라인 374 (SELL 로직)**: 키움 API의 체결 가격(`price`)과 포트폴리오 모듈에서 계산된 실제 수익률(`res_rate`), 엔진 분석 사유(`reason`)를 직접 매핑합니다.
- **코드 발췌**:
  ```python
  db_logger.log_trade(t['name'], "SELL", price, rate=float(res_rate), reason=reason, ma_status=ma_signal, resource_prices=res_prices)
  ```

### 2) TechnicalEngine (기술적 분석 엔진)
- **파일**: [technical_engine.py](file:///d:/Antigravity/Stock_Game/app/engines/technical_engine.py)
- **라인 439 (SELL 로직)**: 실시간 주가(`current_price`)와 객체에서 계산된 `res['수익률']`, 기술적 지표 근거(`sell_reason`)를 기록합니다.
- **코드 발췌**:
  ```python
  db_logger.log_trade(name, "SELL", current_price, rate=float(res['수익률']), reason=sell_reason, rsi_value=rsi_val, ma_status=ma_status)
  ```

## 2. BUY/SELL 내역 통합 출력 로직
- **파일**: [api_routes.py](file:///d:/Antigravity/Stock_Game/app/routes/api_routes.py)
- **라인 265-271**: `get_trade_logs()`를 통해 DB에서 BUY와 SELL 구분 없이 최신순으로 모든 기록을 가져와 리스트에 출력합니다.
- **UI 반영**: `dashboard.js`에서 `type`이 '매수'일 때 빨간색, '매도'일 때 파란색으로 구분하여 시각화합니다.

## 3. 최종 결론
- 엔진 가동 시 발생하는 모든 데이터는 더 이상 임의의 값이 아닌, **시장 실시간 데이터와 엔진의 분석 결과**가 기록됩니다.
- 본 리포트의 전체 내용을 n8n으로 전송하여 Gemini 노드가 실제 코드의 정합성을 검토할 수 있도록 합니다.