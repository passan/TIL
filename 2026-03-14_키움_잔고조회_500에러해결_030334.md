{
  "title": "키움 API 500 에러 해결: 예외 처리를 통한 시스템 견고성 확보 및 데이터 무결성 보장 전략",
  "date": "2026-03-14",
  "tags": ["kiwoom", "bridge", "fix", "python", "dashboard", "error-handling", "api"],
  "status": "Resolved",
  "content": {
    "symptom": "키움 32비트 브릿지 서버에서 `GET /api/account` 엔드포인트 호출 시 500 Internal Server Error가 주기적으로 발생하였습니다. 이로 인해 운영 대시보드에 표시되는 '실투자 예수금', '총 매입금액', '총 평가손익'과 같은 핵심 금융 지표들이 정상적으로 로드되지 않고 '-' (하이픈)으로 출력되는 사용자 경험 저하 문제가 관찰되었습니다.",
    "root_cause": "근본 원인 분석 결과, 다음과 같은 세 가지 주요 문제점이 식별되었습니다:\n1.  **계좌 목록 조회 로직의 취약성**: `GetLoginInfo(\"ACCLIST\")` 호출 시 반환되는 계좌 목록 문자열이 비어있을 경우, `.split(';')[0]`와 같이 첫 번째 요소를 직접 참조하는 로직에서 `IndexError`가 발생할 잠재적 위험이 확인되었습니다. 이는 키움 API가 예상치 못한 형식의 빈 문자열을 반환할 때 시스템이 불안정해질 수 있음을 의미합니다.\n2.  **TR 데이터 파싱의 견고성 부족**: 계좌에 잔고가 전혀 없는 상태(예: 신규 모의투자 계좌)에서 `GetCommData` 함수가 빈 문자열을 반환할 때, 해당 값을 `int()`로 직접 변환하려는 시도에서 `ValueError`가 발생하였습니다. 이는 데이터의 유효성을 사전 검증하지 않고 타입을 강제 변환하는 과정에서 발생한 데이터 무결성 문제입니다.\n3.  **TR 응답 처리의 불완전성**: 키움 서버로의 TR(Transaction Request) 요청 후 `tr_event.wait()` 호출 시, 네트워크 지연, 서버 응답 없음 또는 기타 예상치 못한 오류로 인해 데이터가 정상적으로 수신되지 않을 경우 `None`이 반환되었습니다. 이때 상위 레이어에서 이 `None` 값에 대한 적절한 폴백(fallback) 처리 로직이 부재하여, 최종적으로 500 Internal Server Error로 전파되는 현상이 분석되었습니다.",
    "solution": "시스템의 안정성 및 데이터 신뢰성을 확보하기 위해 다음과 같은 다각적인 해결 방안이 구현되었습니다:\n1.  **로그인 계좌 정보 유효성 검증 강화**: `GetLoginInfo(\"ACCLIST\")` 호출 결과로 반환된 계좌 목록 문자열의 유효성을 명시적으로 검증하고, 비어있을 경우 안전하게 기본값 처리를 수행하도록 로직을 수정하여 `IndexError` 발생 가능성을 원천적으로 차단하였습니다.\n2.  **TR 데이터 필드 매핑 및 예외 처리 로직 보강**: `d+2추정예수금`, `예수금`, `추정예탁자산` 등 여러 유사 필드명에 대해 순차적으로 데이터를 조회하고, 각 필드 값 변환 시 `try-except` 블록을 활용하여 `ValueError` 및 `TypeError`를 포괄적으로 처리하도록 로직을 강화하였습니다. 파싱 실패 시에는 안전한 기본값(0)을 할당함으로써 데이터 무결성을 확보하였습니다.\n3.  **키움 서버 응답에 대한 폴백 로직 구현**: 키움 서버로부터 \"조회내역이 없습니다\" 메시지(또는 특정 에러 코드 571578)를 수신하거나, TR 요청 후 `None` 값이 반환될 경우, 즉시 500 에러를 발생시키는 대신 0으로 채워진 기본 계좌 데이터 객체를 반환하도록 개선하였습니다. 이를 통해 부분적인 실패가 전체 시스템 오류로 전파되는 것을 방지하였습니다.\n4.  **프론트엔드 Nullish Coalescing 연산자 적용**: 대시보드를 렌더링하는 `dashboard.js` 코드 내에서 서버로부터 전달되는 데이터가 `null` 또는 `undefined`일 경우 `??` (Nullish Coalescing) 연산자를 활용하여 자동으로 0으로 처리하도록 방어적 프로그래밍을 적용하였습니다. 이는 백엔드 데이터가 부분적으로 누락되더라도 사용자 인터페이스가 견고하게 작동하도록 보장합니다.",
    "code_snippet": "```python\n# app/engines/kiwoom_connector.py 수정 발췌\ntry:\n    deposit = get_data(\"d+2추정예수금\") or get_data(\"예수금\") or get_data(\"추정예탁자산\") or \"0\"\n    self.tr_data['account'] = {\n        \"deposit\": int(deposit),\n        \"total_buy\": int(get_data(\"총매입금액\") or \"0\"),\n        \"total_profit\": int(get_data(\"총평가손익금액\") or \"0\"),\n        \"yield_rate\": float(get_data(\"총수익률(%)\") or \"0.0\")\n    }\nexcept (ValueError, TypeError) as e:\n    logger.error(f\"Failed to parse account data: {e}\")\n    self.tr_data['account'] = None\n```"
  },
  "learning_point": "이번 사례는 외부 API 연동 시 발생할 수 있는 데이터 무결성 문제, 응답 불확실성, 그리고 예상치 못한 엣지 케이스 처리의 중요성을 강조합니다. 특히, 방어적 프로그래밍(Defensive Programming) 원칙을 적용하여 입력 데이터의 유효성을 항상 검증하고, 다중 폴백(Multiple Fallbacks) 전략을 통해 잠재적 오류 발생 지점에서 시스템의 안정성을 확보하는 것이 중요함을 시사합니다. 또한, 백엔드와 프론트엔드 양단에서 데이터 누락 및 오류에 대한 탄력적인 처리 로직을 구현함으로써, 사용자 경험을 저해하지 않으면서 시스템의 견고성을 극대화할 수 있다는 점을 배울 수 있었습니다."
}