
{
  "title": "키움증권 API 연동 시 500 에러 해결: 견고한 계좌 데이터 조회 시스템 구축",
  "date": "2026-03-14",
  "tags": ["kiwoom", "bridge", "fix", "python", "dashboard"],
  "status": "Resolved",
  "content": {
    "symptom": "키움증권 32비트 브릿지 서버를 통해 계좌 정보 조회 API(`/api/account`)를 호출할 때 500 Internal Server Error가 발생했습니다. 이로 인해 사용자 대시보드에서는 '실투자 예수금', '총 매입금액', '총 평가손익'과 같은 주요 금융 지표들이 정상적으로 표시되지 않고 '-' (하이픈)으로 나타나는 현상이 관찰되었습니다.",
    "root_cause": "문제의 주요 원인은 세 가지였습니다. 첫째, `GetLoginInfo(\"ACCLIST\")` 호출 시 반환되는 계좌 목록 문자열이 비어있는 경우, `.split(';')[0]`와 같은 직접적인 인덱스 접근이 `IndexError`를 유발할 수 있었습니다. 둘째, 잔고가 없는 계좌(예: 신규 모의투자 계좌)에 대해 `GetCommData` 함수가 빈 문자열을 반환했을 때, 이를 `int()`로 변환하려 시도하면서 `ValueError`가 발생했습니다. 셋째, TR(거래 요청) 처리 과정에서 응답 지연이나 데이터 수신 실패로 인해 `tr_event.wait()`가 `None`을 반환할 경우, 이를 처리하는 상위 레이어에 적절한 폴백 로직이 부재하여 최종적으로 API 500 Internal Server Error로 이어졌습니다.",
    "solution": "근본적인 문제를 해결하기 위해 다음과 같은 조치들을 적용했습니다. 첫째, `GetLoginInfo`를 통해 획득한 계좌 목록 문자열의 유효성을 명시적으로 검증하여 안전하게 첫 번째 계좌 정보를 추출하도록 로직을 수정했습니다. 둘째, `d+2추정예수금`, `예수금`, `추정예탁자산` 등 여러 잠재적 필드명을 순차적으로 시도하는 멀티 필드 매핑 전략을 도입하고, 데이터 변환 시 `try-except` 블록을 활용하여 `ValueError`를 방지하고 기본값 0을 설정함으로써 견고성을 높였습니다. 셋째, 키움 서버로부터 '조회내역이 없습니다'와 같은 빈 응답이나 특정 에러 코드(예: 571578)가 수신될 경우, 즉시 500 에러를 반환하는 대신 0으로 채워진 기본 데이터 객체를 폴백으로 제공하도록 API 로직을 개선했습니다. 마지막으로, 프론트엔드의 `dashboard.js`에서는 `nullish coalescing (??)` 연산자를 적용하여 서버로부터 데이터가 누락되더라도 0으로 안전하게 렌더링되도록 방어 코드를 추가했습니다.",
    "code_snippet": 🟡# app/engines/kiwoom_connector.py 수정 발췌
try:
    deposit = get_data("d+2추정예수금") or get_data("예수금") or get_data("추정예탁자산") or "0"
    self.tr_data['account'] = {
        "deposit": int(deposit),
        "total_buy": int(get_data("총매입금액") or "0"),
        "total_profit": int(get_data("총평가손익금액") or "0"),
        "yield_rate": float(get_data("총수익률(%)") or "0.0")
    }
except (ValueError, TypeError) as e:
    logger.error(f"Failed to parse account data: {e}")
    self.tr_data['account'] = None🟡
  },
  "learning_point": "이 이슈를 통해 외부 API 연동 시 발생할 수 있는 다양한 엣지 케이스(빈 응답, 데이터 형식 불일치, 응답 지연 등)를 깊이 이해하고, 이에 대한 견고한 방어 로직을 구현하는 것이 중요함을 깨달았습니다. 백엔드에서 데이터 유효성을 철저히 검증하고 안전한 폴백을 제공하는 것은 물론, 프론트엔드에서도 `nullish coalescing`과 같은 방어적인 렌더링 기법을 적용하여 사용자 경험을 향상시키는 통합적인 접근이 필수적입니다."
}
