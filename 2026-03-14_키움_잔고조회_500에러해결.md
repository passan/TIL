
{
  "title": "키움증권 API 연동 500 에러 해결기: 안정적인 금융 대시보드 데이터 처리를 위한 견고한 브릿지 서버 구축",
  "date": "2026-03-14",
  "tags": ["kiwoom", "bridge", "fix", "python", "dashboard"],
  "status": "Resolved",
  "content": {
    "symptom": "키움증권 32비트 브릿지 서버의 계좌 정보 조회 API(`GET /api/account`) 호출 시 예기치 않은 500 Internal Server Error가 발생했습니다. 이로 인해 사용자 대시보드에 '실투자 예수금', '총 매입금액', '총 평가손익'과 같은 핵심 금융 지표들이 정상적으로 표시되지 않고 '-'으로 나타나는 문제가 확인되었습니다.",
    "root_cause": "문제의 근본 원인은 여러 데이터 처리 과정에서의 예외 미처리 및 견고하지 못한 로직 설계에 있었습니다. 첫째, `GetLoginInfo('ACCLIST')` 호출 결과로 계좌 목록이 비어있을 경우, 이를 안전하게 처리하지 못하고 `.split(';')[0]`에 직접 접근하려 시도하여 인덱스 에러가 발생했습니다. 둘째, 잔고가 없는 신규 모의투자 계좌 등에서 `GetCommData` 함수가 빈 문자열을 반환했을 때, 이를 `int()`로 직접 변환하려다 `ValueError`가 발생하는 취약점이 존재했습니다. 마지막으로, TR 요청 후 데이터 수신을 대기하는 `tr_event.wait()` 과정에서 응답 지연이나 실패로 `None`이 반환될 경우, 이를 적절히 처리할 폴백(fallback) 로직이 없어 상위 API 레이어에서 최종적으로 500 Internal Server Error로 전파되는 문제가 발생했습니다.",
    "solution": "이러한 문제들을 해결하기 위해 다음과 같은 다각적인 접근 방식을 적용했습니다. 첫째, 키움증권 로그인 정보에서 계좌 목록을 가져올 때, 반환된 문자열의 유효성을 명시적으로 검증하여 안전하게 첫 번째 계좌 정보를 추출하도록 로직을 개선했습니다. 둘째, 'd+2추정예수금', '예수금', '추정예탁자산'과 같은 여러 가능한 필드명을 순차적으로 시도하고, 데이터를 `int()`로 변환하는 과정에서 발생할 수 있는 `ValueError`를 `try-except` 블록으로 포괄하여 기본값 0을 반환하도록 처리 견고성을 높였습니다. 셋째, 키움 서버로부터 '조회내역이 없습니다'와 같은 특정 에러 메시지(예: 571578)를 수신할 경우, 무작정 500 에러를 반환하는 대신 0으로 채워진 기본 계좌 데이터 객체를 제공하여 API의 안정성을 확보했습니다. 마지막으로, 클라이언트 측 `dashboard.js`에서는 서버 데이터 누락 상황에 대비하여 `nullish coalescing (??)` 연산자를 활용, 데이터가 `null`이거나 `undefined`일 경우 안전하게 0으로 렌더링되도록 프론트엔드 방어 코드를 추가했습니다.",
    "code_snippet": "# app/engines/kiwoom_connector.py 수정 발췌
try:
    deposit = get_data("d+2추정예수금") or get_data("예수금") or get_data("추정예탁자산") or "0"
    self.tr_data['account'] = {
        "deposit": int(deposit),
        "total_buy": int(get_data("총매입금액") or "0"),
        "total_profit": int(get_data("총평가손익금액") or "0"),
        "yield_rate": float(get_data("총수익률(%)") or "0.0")
    }
except (ValueError, TypeError) as e:
    logger.error(f"Failed to parse account data: {e}")
    self.tr_data['account'] = None"
  },
  "learning_point": "이번 이슈를 통해 외부 금융 API 연동 시 데이터의 유효성 검증과 예외 처리를 얼마나 견고하게 설계해야 하는지 재확인할 수 있었습니다. 특히, 다양한 잠재적 실패 지점(빈 데이터, 타입 변환 에러, 응답 지연)을 사전에 인지하고, 서버와 클라이언트 양단에서 방어적인 코드를 작성하여 사용자 경험에 미치는 영향을 최소화하는 것이 중요합니다. 이는 안정적인 시스템 운영을 위한 필수적인 개발 원칙입니다."
}
